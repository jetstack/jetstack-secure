cluster-name: my-cluster

data-gatherers:
  k8s/pods:
    kubeconfig: ~/.kube/config

package-sources:
- type: local
  dir: preflight-packages/

enabled-packages:
  - "examples.jetstack.io/pods_basic"

outputs:
- type: local
  path: ./output
  format: json
- type: cli
